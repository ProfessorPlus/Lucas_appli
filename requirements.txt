# Versions figées : sans pin, Streamlit Cloud a résolu pandas 3.0.5 + pyarrow 25
# (rétrogradé de force en 24 par la plateforme) -> crash natif au démarrage
# (segfault / double free). Ne pas repasser en ">=" sans revalider un déploiement.
streamlit==1.53.1
pyyaml==6.0.3
requests==2.32.5
stripe==14.3.0
reportlab==4.4.9
google-auth>=2.0.0
google-api-python-client>=2.0.0

# Stack native tirée par Streamlit : c'est cette combinaison qui doit rester cohérente
pandas==2.3.3
numpy==2.4.2
pyarrow==23.0.0
